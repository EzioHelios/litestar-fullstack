# Development Helpers

Local-only helpers for manual app setup and server debugging.

These are not part of the application runtime. Prefer the normal project commands (`app run`, `make start-infra`, `make types`, `make build-assets`) for everyday development.
