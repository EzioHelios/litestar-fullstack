# Database Diagnostics

Small database inspection scripts collected from migration/debugging work.

Use `OLD_DB_URL` for legacy source database checks and `NEW_DB_URL` or the app `DATABASE_URL` for target database checks. Scripts that parse a dump file expect `SQL_DUMP_PATH`.

Keep command output, logs, and ad hoc result files out of Git.
